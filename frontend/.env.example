# =========================================
# FRONTEND POS - VARIABLES DE ENTORNO
# =========================================
# 🚨 IMPORTANTE: Copie este archivo como .env antes de iniciar el frontend
# cp .env.example .env

# =========================================
# HOST - REQUERIDO PARA ACCESO MULTI-DISPOSITIVO
# =========================================
# 🚨 CRÍTICO: Esta configuración es NECESARIA para acceder desde dispositivos móviles
# HOST=0.0.0.0 permite que el frontend sea accesible desde otros dispositivos
# en la misma red local (tablets, móviles, otros PCs).
# 
# Sin esto, el frontend solo será accesible en localhost del mismo equipo.
# NO comente esta línea si desea acceder desde dispositivos móviles.
HOST=0.0.0.0

# =========================================
# API DEL BACKEND
# =========================================
# En desarrollo local, se detecta automáticamente.
# - Si accede desde localhost:3000, usará localhost:3001
# - Si accede desde IP local (ej: 192.168.1.100:3000), usará 192.168.1.100:3001
# 
# Por lo tanto, en desarrollo local puede dejar este valor vacío o comentado.
# REACT_APP_API_URL=

# Producción (reemplazar con tu URL de Railway):
# REACT_APP_API_URL=https://tu-backend.railway.app/api

# =========================================
# PASOS PARA ACCESO DESDE MÓVILES
# =========================================
# 1. Copie este archivo: cp .env.example .env
# 2. Asegúrese de que HOST=0.0.0.0 esté descomentado
# 3. Inicie el backend: npm run start:backend (en la raíz del proyecto)
# 4. Inicie el frontend: npm run start:frontend (en la raíz del proyecto)
# 5. El backend mostrará las IPs disponibles (ej: 192.168.1.100)
# 6. En su móvil/tablet, abra: http://192.168.1.100:3000
# 7. El frontend detectará automáticamente el backend en http://192.168.1.100:3001
