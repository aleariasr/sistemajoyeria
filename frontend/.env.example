# =========================================
# FRONTEND POS - VARIABLES DE ENTORNO
# =========================================
# Copiar este archivo como .env y configurar los valores

# =========================================
# API DEL BACKEND (REQUERIDO en producción)
# =========================================
# Esta variable es OBLIGATORIA para conectar con el backend en producción.
# En desarrollo local, se detecta automáticamente.

# Desarrollo local (detección automática):
# - Si accede desde localhost:3000, usará localhost:3001
# - Si accede desde IP local (ej: 192.168.1.100:3000), usará 192.168.1.100:3001
# Por lo tanto, en desarrollo local puede dejar este valor vacío o comentado.
REACT_APP_API_URL=http://localhost:3001/api

# Producción (reemplazar con tu URL de Railway):
# REACT_APP_API_URL=https://tu-backend.railway.app/api

# =========================================
# DESARROLLO LOCAL - RED LOCAL
# =========================================
# Para acceder desde dispositivos móviles en la misma red:
# 1. Deje REACT_APP_API_URL vacío o comentado
# 2. Inicie el backend con: npm run start:backend
# 3. Inicie el frontend con: npm run start:frontend
# 4. El frontend detectará automáticamente la IP y se conectará al backend
#
# Alternativamente, puede configurar la IP manualmente:
# REACT_APP_API_URL=http://192.168.1.100:3001/api

# Host para acceso desde otros dispositivos en red local
HOST=0.0.0.0
