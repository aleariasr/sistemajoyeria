# Host del servidor de desarrollo
# 0.0.0.0 permite acceso desde otros dispositivos en la red local
HOST=0.0.0.0

# URL de la API del backend
# IMPORTANTE: Para acceso multi-dispositivo en red local, NO configures esta variable
# El sistema detectará automáticamente la IP correcta del servidor
# Solo configura REACT_APP_API_URL si necesitas apuntar a un servidor específico
# Ejemplo para desarrollo local: REACT_APP_API_URL=http://localhost:3001/api
# Ejemplo para producción: REACT_APP_API_URL=https://api.midominio.com/api

# Otras configuraciones pueden agregarse aquí según sea necesario
