# Puerto del servidor (default: 3001)
PORT=3001

# Host del servidor (default: 0.0.0.0 para acceso multi-dispositivo)
# 0.0.0.0 = escucha en todas las interfaces de red (recomendado)
# 127.0.0.1 = solo localhost (no accesible desde otros dispositivos)
HOST=0.0.0.0

# Ambiente de ejecución (development | production)
NODE_ENV=development

# Secret para las sesiones (CAMBIAR EN PRODUCCIÓN - usar valor largo y aleatorio)
SESSION_SECRET=your-secure-secret-key-change-in-production

# Redis (solo en producción, opcional)
# REDIS_URL=redis://usuario:password@host:port

# ⚠️ IMPORTANTE: URL del frontend en producción
# Configurar en Railway para que funcione el login cross-origin
# Cuando se configura, las cookies usan sameSite: 'none' y secure: true
FRONTEND_URL=https://your-frontend-domain.vercel.app

# Límite de tamaño para peticiones JSON (default: 10mb)
# REQUEST_SIZE_LIMIT=10mb

# Configuración de Supabase (REQUERIDO)
# ⚠️ IMPORTANTE: Configura tus propias credenciales de Supabase
# Crea un proyecto en https://supabase.com y obtén estas credenciales
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# Configuración de Cloudinary (REQUERIDO para imágenes)
# ⚠️ IMPORTANTE: Configura tus propias credenciales de Cloudinary
# Crea una cuenta en https://cloudinary.com y obtén estas credenciales
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
