# =========================================
# BACKEND - VARIABLES DE ENTORNO
# =========================================
# Copiar este archivo como .env y configurar los valores

# Puerto del servidor (Railway lo proporciona automáticamente)
PORT=3001

# Host del servidor (0.0.0.0 permite conexiones desde cualquier IP)
HOST=0.0.0.0

# Ambiente de ejecución
# - development: Logs detallados, CORS más permisivo, sin HTTPS requerido
# - production: Logs mínimos, CORS estricto, HTTPS requerido para cookies
NODE_ENV=development

# Secret para las sesiones (CAMBIAR EN PRODUCCIÓN - usar valor largo y aleatorio)
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secure-secret-key-change-in-production

# =========================================
# PRODUCCIÓN - RAILWAY + VERCEL
# =========================================

# URLs del frontend en producción (REQUERIDO en producción)
# Puede incluir múltiples URLs separadas por comas para soportar:
# - Frontend POS (React)
# - Storefront (Next.js)
# - Cualquier otro frontend
# Ejemplo con múltiples URLs:
# FRONTEND_URL=https://pos.vercel.app,https://tienda.vercel.app
FRONTEND_URL=

# URL del storefront (opcional, se puede incluir en FRONTEND_URL)
# STOREFRONT_URL=https://tienda.vercel.app

# =========================================
# SUPABASE (REQUERIDO)
# =========================================
# Crear proyecto en: https://supabase.com
# Ir a Settings > API para obtener estas credenciales

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# =========================================
# CLOUDINARY (REQUERIDO para imágenes)
# =========================================
# Crear cuenta en: https://cloudinary.com
# Ir a Dashboard para obtener estas credenciales

CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret

# =========================================
# EMAIL CONFIGURATION (Para pedidos online y comprobantes)
# =========================================
# Configuración para enviar emails transaccionales usando Resend API
# 
# Resend es un servicio de emails transaccionales que funciona vía REST API
# (no requiere puertos SMTP, por lo que funciona perfectamente en Railway)
#
# Configuración:
# 1. Crear cuenta en: https://resend.com
# 2. Verificar tu dominio en el dashboard
# 3. Obtener tu API key
# 4. Configurar el email FROM con tu dominio verificado

# API Key de Resend (REQUERIDO para enviar emails)
# Obtener en: https://resend.com/api-keys
RESEND_API_KEY=re_123456789

# Email remitente (debe usar dominio verificado en Resend)
EMAIL_FROM=ventas@cueroyperla.com

# Nombre que aparecerá como remitente
EMAIL_FROM_NAME=Cuero&Perla

# Email Reply-To (a dónde llegan las respuestas de los clientes)
EMAIL_REPLY_TO=contacto@cueroyperla.com

# Email del administrador (para recibir notificaciones de nuevos pedidos)
ADMIN_EMAIL=cueroyperla@icloud.com

# Store Configuration (para emails)
STORE_NAME=Cuero&Perla
STORE_URL=https://sistemainterno.cueroyperla.com
STORE_PHONE=+506-7269-7050

# =========================================
# REDIS (OPCIONAL - solo producción)
# =========================================
# Para persistencia de sesiones en Railway
# Railway proporciona Redis como add-on
# REDIS_URL=redis://usuario:password@host:port
